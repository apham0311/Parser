Instructions on how to run our code:
1) To compile our code, run the makefile using the command "make"
2) To run and implement, type in ./a.out input.txt. The following directives are supported:

-l : print the list and table of lexemes/tokens (output) to the screen
-s : print the symbol table
-a : print the generated assembly code (parser/codegen output) to the screen
-v : print virtual machine execution trace (HW1 output) to the screen
<filename>.txt : input file name, for e.g. input.txt